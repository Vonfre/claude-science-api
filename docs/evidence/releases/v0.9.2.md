# SciPort v0.9.2 发布证据

- 日期：2026-09-11。类型：绑定版本的发布证据，不是当前运行状态合同。
- 目标：`Vonfre/claude-science-api`，immutable `v0.9.2`，macOS Apple Silicon / arm64。
- Feature baseline: `56c8bf10daa5289940f25f138e27898c0e98788b`. Final release source: `73c47c34daa73327b6a23084434c0c1d9dbc3cc5`, consistently bound by tag, Actions and BUILD_INFO.txt. Later evidence-only commits do not move the release tag.
- 授权：维护者要求直接构建发布；不覆盖旧公开版本，不安装或启动本机 App。
- 源码层：功能候选聚焦 JS 14/14、frontend、Rust app_update 7/7、home_layout 6/6、s2_stop_all 1/1、native_exit 3 PASS / 1 ignored、签名正负例通过；两轮独立审查完成。此记录不把旧候选检查外推为发布 commit 的完整门禁。
- 完整 source gate：NOT-RUN。真实 App 首启、已安装 App 升级、Science 目录行为和真实 provider：未验证。
- 发布配置：已建立正式持久更新签名密钥，GitHub 配置存在；私钥未提交仓库或写入日志。
- Build and public release: GitHub Actions run `34597541793` SUCCESS; published 2026-09-11 12:20:56 UTC (20:20:56 Asia/Shanghai), not draft or prerelease; latest is v0.9.2.
- Apple signing: final ad-hoc signing and CI `codesign --verify --deep --strict` passed. Not Developer ID signed or notarized; Gatekeeper unverified. The separate update signature does not replace Apple signing or notarization.
- 功能边界：HOME 外路径仍需 Science granted-root 授权；过期官方连接器会话未修复。v0.9.1 及更早版本需手动安装本版一次。

## Final distribution verification

- [Release](https://github.com/Vonfre/claude-science-api/releases/tag/v0.9.2) / [Actions run](https://github.com/Vonfre/claude-science-api/actions/runs/34597541793).
- All six public assets downloaded: `SciPort_0.9.2_aarch64.dmg`, `SciPort_0.9.2_aarch64.app.tar.gz`, its `.sig`, `latest.json`, `SHA256SUMS`, and `BUILD_INFO.txt`.
- DMG SHA-256: `0551c77393e293f624fa44d81707ff2e3f3b51675f0aeee1bf5810ca190075fe`.
- Update archive SHA-256: `704030cbbfbdfdbf00c53c011701bff86e1de7f74e21215ddef37b91d5642e67`.
- All four SHA256SUMS entries match public downloads; the downloaded update archive passed local signature verification with the production public key.
- Manifest version, darwin-aarch64 URL, and detached signature agree. Archive Info.plist reports version 0.9.2 and identifier com.csswitch.menubar. Both executable hashes match BUILD_INFO.txt.
- CI verified DMG payload allowlist, arm64 architecture, App signature, and both executable hashes against the source bundle. The update archive was packaged from that same final-signed App.
- Release preparation checks passed: version consistency, frontend, focused JS 14/14, release notes, document governance 4/4, shell syntax, and diff whitespace. Full source gate and runtime layers remain unverified as disclosed above.
